services:
  - type: web
    name: rizz-luxe
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NPM_FLAGS
        value: --legacy-peer-deps
